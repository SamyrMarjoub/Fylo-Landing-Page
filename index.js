var windowWidth = window.innerWidth;
const img = document.querySelector('.img-curva')
if(windowWidth <=975){
    img.setAttribute('src', './images/bg-curve-mobile.svg')
    console.log(windowWidth)
}
//
